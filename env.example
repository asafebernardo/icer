# --- Frontend (Vite) ---
# Copie para `.env` ou `.env.local`. Prefixo VITE_ é exposto ao browser.

# Ativa login real contra o servidor Node (`server/index.js`) e cookies httpOnly.
# Valores aceites (igual ao proxy Vite): true | 1 | yes | on
VITE_USE_SERVER_AUTH=true

# --- Desenvolvimento local (`npm run dev:all`) ---
# Use http://localhost:5173 (ou a porta do Vite). Não defina VITE_APP_BASE_URL no .env
# local — isso pode encaminhar pedidos da API para o site em produção.
# ICER_DEV_PUBLIC_BASE_URL=http://localhost:5173
# Login Google em localhost: no Google Cloud Console, adicione também o redirect URI:
#   http://localhost:5173/api/auth/google-login/callback
# (além do URI de produção). Use sempre o mesmo hostname (localhost ou 127.0.0.1).

# URL de uma API upstream (opcional) quando NÃO usa apenas o Node.
# Em dev com VITE_USE_SERVER_AUTH=true, o Vite proxy de `/api` vai para o Node local.
# Se aparecer 504 «Gateway Timeout» na UI com Mongo lento (Atlas), aumente (ms), ex.: 600000.
# VITE_DEV_API_PROXY_TIMEOUT_MS=300000
# VITE_APP_BASE_URL=https://your-upstream.example.com

# Upload de imagens só no browser (data URL)
# VITE_LOCAL_IMAGE_UPLOAD=true

# --- Servidor Node (`npm run dev:server` / systemd) ---
# MongoDB — escolha UM dos modos (local OU Atlas).
MONGODB_DB_NAME=icer
#
# --- Modo local (MongoDB na máquina, porta 27017) ---
MONGODB_URI=mongodb://127.0.0.1:27017
#
# --- Modo Atlas (Opção A — recomendado): comente ou apague MONGODB_URI acima e use: ---
# MONGODB_SRV_HOST=cluster0.xxxxx.mongodb.net
# MONGODB_USER=...   ou   MONGODB_SRV_USER=...
# MONGODB_PASSWORD=...   ou   MONGODB_SRV_PASSWORD=...
# MONGODB_DB_NAME=icer   ou   MONGODB_SRV_DATABASE=icer
# (O código monta mongodb+srv://… com encoding correto.)
# Não use MONGODB_URI=$VAR — o Node/dotenv não expande $ como no shell.

# Ficheiros de media (imagens, PDF, vídeo) no disco do servidor — só metadados no MongoDB
# Em produção: use um caminho ABSOLUTO fora da pasta que o deploy substitui (ex.: /var/lib/icer/uploads),
# para cópias/rsync não apagarem os ficheiros. Ver DEPLOY.md.
# ICER_UPLOAD_DIR=server/uploads
# ICER_UPLOAD_MAX_MB=80
# Migração idempotente de categorias de posts no arranque da API (workspace + body_json). Predefinição: activa.
# Após a primeira migração em produção, defina false para arranques mais rápidos (ver UPLOADS-E-DEPLOY.md).
# ICER_RUN_POST_CATEGORY_MIGRATION=false
# Pasta de ficheiros de log no disco (relativa à raiz do projeto se não for absoluta). Predefinição: logs
# ICER_LOG_DIR=logs
# ICER_FILE_PUBLIC_READ=true

# GitHub: em Admin → Atualizações, cada commit no branch predefinido do repo conta como uma «versão».
# Sem ICER_GITHUB_REPO, usa por defeito https://github.com/asafebernardo/icer
# Opcional: listar commits de outro branch (senão usa o default_branch do GitHub).
# ICER_GITHUB_COMMITS_BRANCH=develop
# Defina ICER_GITHUB_REPO=owner/repo (ou URL) para outro fork. Repositório privado: ICER_GITHUB_TOKEN.
# ICER_GITHUB_DISABLED=1  # desativa chamadas à API (ex.: testes `npm run test:server`)
# ICER_GITHUB_REPO=outro-org/icer
# ICER_GITHUB_TOKEN=ghp_...

# --- Homologação (staging) ---
# No painel do servidor (EasyPanel, etc.), defina pelo menos:
#   ICER_ENV=homolog
#   VITE_USE_SERVER_AUTH=true   (build do frontend — ver Dockerfile ARG)
#   ICER_PUBLIC_BASE_URL=https://hml.seu-dominio.com   (URL pública, sem barra final)
#   MONGODB_* ou MONGODB_URI (mesma base onde existem os utilizadores)
# Em homologação o bloqueio por tentativas falhadas de login fica desligado.
# Se o site homolog for só HTTP, o cookie de sessão deixa de exigir Secure automaticamente
# quando ICER_PUBLIC_BASE_URL começa por http:// (ou force ICER_COOKIE_SECURE=false).
#
# Contas seed de login (só com ICER_HOMOLOG=true):
#   Credenciais predefinidas no código (admin@example.com / user@example.com).
#   Opcional: sobrescrever com ICER_ADMIN_* e ICER_USER_* abaixo.
# ICER_ENV=homolog
# ICER_HOMOLOG=true
# Reinicie `npm run dev:all` após alterar. Expõe menus admin bloqueados no frontend (homolog).
# Conta seed: admin@example.com / Change-me-Min12! (ver server/homologSeed.js)
# ICER_ENFORCE_SINGLE_SESSION=false
# ICER_COOKIE_SECURE=false
# ICER_ADMIN_EMAIL=admin@example.com
# ICER_ADMIN_FULL_NAME=Administrador
# ICER_ADMIN_PASSWORD=Change-me-Min12!
# ICER_USER_EMAIL=user@example.com
# ICER_USER_FULL_NAME=Outro administrador
# ICER_USER_PASSWORD=Change-me-Min12!

# Porta interna (reverse proxy aponta /api para aqui)
PORT=3001
# ICER_SERVER_PORT=3001
#
# Em Docker/EasyPanel: com `PORT` definido, o servidor escuta em 0.0.0.0 (acessível de fora).
# Para forçar manualmente: HOST=0.0.0.0 ou HOST=127.0.0.1
# HOST=

# Cookie de sessão (opcional; padrão em auth.js)
# ICER_SESSION_COOKIE_NAME=icer_session
# ICER_SESSION_SECRET=...   # se usar assinatura no futuro

# Uma sessão ativa por conta (predefinido: ativo). `false` permite vários dispositivos sem bloqueio.
# Na UI de login também existe «encerrar outra sessão» com a mesma palavra-passe.
# ICER_ENFORCE_SINGLE_SESSION=false

# Seed de contas: só activo com ICER_HOMOLOG=true (valores predefinidos em server/homologSeed.js).
# Em produção deixe ICER_HOMOLOG comentado ou false — não defina estas variáveis.
# ICER_HOMOLOG=true
# ICER_ADMIN_EMAIL=admin@example.com
# ICER_ADMIN_FULL_NAME=Administrador
# ICER_ADMIN_PASSWORD=Change-me-Min12!
# ICER_USER_EMAIL=user@example.com
# ICER_USER_FULL_NAME=Outro administrador
# ICER_USER_PASSWORD=Change-me-Min12!

# --- Base pública (obrigatória para login com Google) ---
# URL do site sem barra final (Redirect URI OAuth = {ICER_PUBLIC_BASE_URL}/api/auth/google-login/callback).
# ICER_PUBLIC_BASE_URL=https://seu-dominio.com
#
# Login com Google: só e-mails listados abaixo podem usar OAuth (têm de existir em `users` no MongoDB).
# ICER_GOOGLE_LOGIN_CLIENT_ID=....apps.googleusercontent.com
# ICER_GOOGLE_LOGIN_CLIENT_SECRET=...
# ICER_GOOGLE_LOGIN_ALLOWED_EMAILS=admin@example.com,outro@example.com

# Encaminhar outras rotas /api/* para API externa, exceto auth/admin/users/files/health
# ICER_UPSTREAM_API=https://your-upstream.example.com

# WebP — qualidade (1–100) para conversão de imagens (predefinido: 82)
# ICER_WEBP_QUALITY=82
#
# Converter imagens estáticas (public/) e uploads do servidor:
#   npm run images:webp:public    # assets em public/ (desenvolvimento)
#   npm run images:webp:uploads   # ficheiros já enviados (produção, usa MongoDB + ICER_UPLOAD_DIR)
#   npm run images:webp           # ambos
# Novos uploads de imagens (JPG/PNG) são convertidos automaticamente para WebP no servidor.
